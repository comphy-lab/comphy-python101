"""Starter: compose validated log evidence."""

from pathlib import Path


def build_evidence(source: Path, output: Path) -> tuple[Path, Path]:
    del source, output
    # TODO: validate once, then write summary.json and log.png.
    raise NotImplementedError
