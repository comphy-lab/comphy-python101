"""Starter: reduce a named CSV schema."""

from pathlib import Path


def summarise_log(path: Path) -> dict[str, float | int]:
    # TODO: validate the header and reduce the named columns.
    raise NotImplementedError
