"""Starter: a thin command-line boundary around a tested reduction."""

from collections.abc import Sequence


def main(arguments: Sequence[str] | None = None) -> int:
    """Write a validated log summary to an explicit output path."""

    del arguments
    # TODO: parse input and --output, then coordinate existing functions.
    raise NotImplementedError
