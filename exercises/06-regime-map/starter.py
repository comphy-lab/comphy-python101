"""Starter: validate and count categorical outcomes."""

from pathlib import Path


def count_outcomes(path: Path) -> dict[str, int]:
    # TODO: validate case IDs and outcomes, then count.
    raise NotImplementedError
