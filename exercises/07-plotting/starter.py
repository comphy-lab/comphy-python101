"""Starter: turn aligned arrays into a deterministic figure."""

from pathlib import Path

from numpy.typing import ArrayLike


def plot_series(time: ArrayLike, values: ArrayLike, output: Path) -> Path:
    """Plot values against time and return the written path."""

    # TODO: validate, label, save, and close the figure.
    raise NotImplementedError
