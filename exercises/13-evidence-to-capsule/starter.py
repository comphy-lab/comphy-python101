"""Starter: compose and verify the complete teaching capsule."""

from pathlib import Path


def reproduce(output: Path) -> list[Path]:
    del output
    # TODO: build the capsule, verify it, and return its declared artefacts.
    raise NotImplementedError
